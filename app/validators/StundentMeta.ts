import vine from "@vinejs/vine";

/**
 * Validates the post's creation action
 */
export const CreateValidatorForStundentMeta = vine.compile(
  vine.object({
      // add here
  })
)


/**
 * Validates the post's update action
 */
export const UpdateValidatorForStundentMeta = vine.compile(
  vine.object({
    
  })
)

