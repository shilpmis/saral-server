import vine from "@vinejs/vine";

/**
 * Validates the post's creation action
 */
export const CreateValidatorForOrganization = vine.compile(
  vine.object({
      // add here
      
  })
)


/**
 * Validates the post's update action
 */
export const UpdateValidatorForOrganization = vine.compile(
  vine.object({
    
  })
)

