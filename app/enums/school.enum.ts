export enum SubscriptionType {
     FREE = 'FREE',
     PREMIUM = 'PREMIUM',
   }
   
export enum SubscriptionStatus {
        PENDING = 'PENDING',
        ACTIVE = 'ACTIVE',
        INACTIVE = 'INACTIVE',
        CANCELED = 'CANCELED',
}